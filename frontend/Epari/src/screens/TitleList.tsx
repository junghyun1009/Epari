import React from 'react';
import {StyleSheet} from 'react-native';
import TitleListStack from '../navigation/TitleListStack';

const TitleList: React.FC = () => {
  return <TitleListStack />;
};

export default TitleList;
